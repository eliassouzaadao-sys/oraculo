# Oraculo - Configuracao
# Copie este arquivo para .env e preencha com suas chaves

# OpenAI API Key (obrigatorio)
OPENAI_API_KEY=sk-sua-chave-aqui

# Modelo LLM (opcional - padrao: gpt-4o-mini)
DEFAULT_MODEL=gpt-4o-mini

# Modelo de Embeddings (opcional - padrao: text-embedding-3-small)
EMBEDDING_MODEL=text-embedding-3-small

# Configuracoes de RAG (opcional)
CHUNK_SIZE=500
CHUNK_OVERLAP=50
TOP_K_RESULTS=5

# Configuracoes do LLM (opcional)
TEMPERATURE=0.7
MAX_TOKENS=2000

# Limite de upload em MB (opcional - padrao: 50)
MAX_FILE_SIZE_MB=50
